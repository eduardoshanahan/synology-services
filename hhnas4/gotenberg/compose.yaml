services:
  gotenberg:
    image: gotenberg/gotenberg:8.25
    container_name: nas-host-gotenberg
    restart: unless-stopped
    ports:
      - "${GOTENBERG_BIND:-0.0.0.0}:${GOTENBERG_PORT:-3001}:3000"
    env_file:
      - .env
