services:
  outline:
    image: docker.getoutline.com/outlinewiki/outline:1.4.0
    container_name: outline
    restart: unless-stopped
    ports:
      - "${OUTLINE_HOST_BIND:-127.0.0.1}:${OUTLINE_PORT:-3010}:3010"
    dns:
      - "${OUTLINE_DNS_1:-192.0.2.10}"
      - "${OUTLINE_DNS_2:-192.0.2.10}"
    extra_hosts:
      - "postgres.internal.example:${OUTLINE_POSTGRES_HOST_IP:-192.0.2.10}"
      - "redis.internal.example:${OUTLINE_REDIS_HOST_IP:-192.0.2.10}"
      - "smtp-relay.internal.example:${OUTLINE_SMTP_HOST_IP:-192.0.2.10}"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - NODE_EXTRA_CA_CERTS=/usr/local/share/ca-certificates/homelab-root-ca.crt
    volumes:
      - "outline_storage_data:/var/lib/outline/data"
      - ./certs/homelab-root-ca.crt:/usr/local/share/ca-certificates/homelab-root-ca.crt:ro
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          node -e 'const http=require("http");const port=process.env.PORT||3010;const hostHeader=new URL(process.env.URL||"http://localhost").host;const hosts=["127.0.0.1","::1"];const probe=(i)=>{if(i>=hosts.length){process.exit(1);return;}const req=http.get({host:hosts[i],port,path:"/_health",headers:{Host:hostHeader,"X-Forwarded-Proto":"https"}},(res)=>{if(res.statusCode===200){process.exit(0);}else{probe(i+1);}});req.on("error",()=>probe(i+1));};probe(0);'
      interval: 30s
      timeout: 10s
      retries: 6
      start_period: 60s

volumes:
  outline_storage_data:
